{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "prepare": "husky install",
    "format": "turbo run format",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest",
    "husky": "^8.0.0"
  },
  "packageManager": "pnpm@7.15.0",
  "name": "repo"
}
